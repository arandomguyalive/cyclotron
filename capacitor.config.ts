import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.km18.abhed',
  appName: 'ABHED',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
